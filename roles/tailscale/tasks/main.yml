---
- name: Install Tailscale with pacman (Arch)
  ansible.builtin.pacman:
    name: tailscale
    state: present
  register: tailscale_install
  when: facts_is_arch

- name: Download Tailscale apt key (Ubuntu)
  ansible.builtin.get_url:
    url: "https://pkgs.tailscale.com/stable/ubuntu/{{ ansible_facts['distribution_release'] }}.noarmor.gpg"
    dest: /usr/share/keyrings/tailscale-archive-keyring.gpg
    mode: '0644'
  when: facts_is_ubuntu

- name: Add Tailscale apt repository (Ubuntu)
  ansible.builtin.apt_repository:
    repo: "deb [signed-by=/usr/share/keyrings/tailscale-archive-keyring.gpg] https://pkgs.tailscale.com/stable/ubuntu {{ ansible_facts['distribution_release'] }} main"
    filename: tailscale
    state: present
  when: facts_is_ubuntu

- name: Install Tailscale with apt (Ubuntu)
  ansible.builtin.apt:
    name: tailscale
    state: present
    update_cache: true
  register: tailscale_install
  when: facts_is_ubuntu

- name: Read Tailscale auth key from 1Password
  become: true
  become_user: "{{ host_user }}"
  ansible.builtin.command:
    cmd: /usr/bin/op read op://sp-dev/tailscale/credential
  register: tailscale_authkey_secret
  no_log: true
  environment:
    OP_SERVICE_ACCOUNT_TOKEN: "{{ lookup('env', 'OP_SERVICE_ACCOUNT_TOKEN') }}"
  when:
    - not facts_tailscale_authed
    - (lookup('env', 'TAILSCALE_AUTHKEY') | trim | length == 0)
    - facts_op_installed
    - (lookup('env', 'OP_SERVICE_ACCOUNT_TOKEN') | trim | length > 0)
  changed_when: false

- name: Warn when OP service account token is missing for Tailscale
  ansible.builtin.debug:
    msg: "OP_SERVICE_ACCOUNT_TOKEN is not set; skipping 1Password Tailscale auth key lookup."
  when:
    - not facts_tailscale_authed
    - (lookup('env', 'TAILSCALE_AUTHKEY') | trim | length == 0)
    - facts_op_installed
    - (lookup('env', 'OP_SERVICE_ACCOUNT_TOKEN') | trim | length == 0)

- name: Warn when Tailscale auth key is unavailable
  ansible.builtin.debug:
    msg: "TAILSCALE_AUTHKEY is unavailable; skipping Tailscale login."
  when:
    - not facts_tailscale_authed
    - (lookup('env', 'TAILSCALE_AUTHKEY') | trim | length == 0)
    - (tailscale_authkey_secret is not defined or tailscale_authkey_secret.stdout | default('') | trim | length == 0)
  tags:
    - tailscale
    - network

- name: Enable and start Tailscale service
  ansible.builtin.systemd:
    name: tailscaled
    state: started
    enabled: true
    daemon_reload: true
  become: true
  when: (tailscale_install is defined and tailscale_install is changed) or facts_tailscale_installed | default(false)

- name: Start Tailscale with auth key
  ansible.builtin.command:
    argv:
      - tailscale
      - up
      - >-
        --authkey={{
          (lookup('env', 'TAILSCALE_AUTHKEY') | trim)
          if (lookup('env', 'TAILSCALE_AUTHKEY') | trim | length > 0)
          else (tailscale_authkey_secret.stdout | default('') | trim)
        }}
      - "--accept-dns={{ tailscale_accept_dns | ternary('true', 'false') }}"
  register: tailscale_up
  changed_when: tailscale_up.rc == 0
  become: true
  no_log: true  # keep the auth key secure
  when:
    - not facts_tailscale_authed
    - >-
      (lookup('env', 'TAILSCALE_AUTHKEY') | trim | length > 0) or
      (tailscale_authkey_secret is defined and tailscale_authkey_secret.stdout | default('') | trim | length > 0)
  tags:
    - tailscale
    - network

- name: Apply Tailscale DNS preference for authenticated node
  ansible.builtin.command:
    argv:
      - tailscale
      - set
      - "--accept-dns={{ tailscale_accept_dns | ternary('true', 'false') }}"
  become: true
  changed_when: false
  when: facts_tailscale_installed and facts_tailscale_authed
  tags:
    - tailscale
    - network
